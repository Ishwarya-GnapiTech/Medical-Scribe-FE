import React from "react";

const StatusBar: React.FC = () => {
  // Returning null completely removes this empty bar container 
  // from rendering above your SoapToolbar view.
  return null;
};

export default StatusBar;